var app = angular
	.module('Bubbles', 
		[
			'nvd3', 
			'ngMaterial', 
			'ngMessages', 
			'checklist-model',
			'ui.utils.masks'
		]
	)
	.config(function($compileProvider, $locationProvider){
	   
});