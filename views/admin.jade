extends layout


block content
	div(ng-controller="BubblesAdminController", flex, layout="column", layout-wrap, layout-padding)

		section(layout="row", flex)
			md-sidenav.md-sidenav-left(md-component-id="left", md-is-locked-open="true", md-whiteframe="4")
				md-content(layout-padding)
					ul.tags
						li(ng-repeat="tag in tags.all")
							md-checkbox(aria-label="{{tag.name}}", ng-value="tag.id")
								span {{tag.name}}
					
					md-input-container.scrapes
						label Scrape Date #1
						md-select(ng-model="scrape1", aria-label="Scrape Date #1")
							md-option(ng-repeat="scrape in scrapes", ng-value="scrape.id") {{scrape.updatedAt | date:'dd/MM/yyyy'}}

					md-input-container.scrapes
						label Scrape Date #2
						md-select(ng-model="scrape2", aria-label="Scrape Date #1")
							md-option(ng-repeat="scrape in scrapes", ng-value="scrape.id") {{scrape.updatedAt | date:'dd/MM/yyyy'}}

			md-content(flex, layout-padding)
				div(ng-include=" '/partials/friendship-graph.tmpl.html' ")

			md-content(flex, layout-padding)
				div(ng-include=" '/partials/friendship-graph.tmpl.html' ")
